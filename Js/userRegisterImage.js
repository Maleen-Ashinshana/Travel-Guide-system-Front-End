/*var loadFile = function (event) {
    var image = document.getElementById("output");
    image.src = URL.createObjectURL(event.target.files[0]);
};
/!*function loadGuiderImage(){

}*!/
var loadImg = function (event) {
    var img = document.getElementById("guide-output");
    img.src = URL.createObjectURL(event.target.files[0]);
};*/
/*function loadImage(event, targetElementId) {
    var element = document.getElementById(targetElementId);
    element.src = URL.createObjectURL(event.target.files[0]);
}

var loadFile = function (event) {
    loadImage(event, "output");
};

var loadImg = function (event) {
    loadImage(event, "guide-output");
};*/

var loadFile = function (event) {
    var image = document.getElementById("output");
    image.src = URL.createObjectURL(event.target.files[0]);
};

/*var loadImg = function (event) {
    var img = document.getElementById("guide-output");
    img.src = URL.createObjectURL(event.target.files[0]);
};*/
